# ご近所・日常生活向けグラフ型ナレッジメモアプリ 仕様書

あなたには、シンプルでありながら柔軟な「グラフ構造（Node + Edge）ベースの日常メモ・予定管理Webアプリ」を実装してもらいます。
以下のデータ構造、UI/UX要件、およびロジック仕様に従って、堅牢で拡張性の高いコードを作成してください。

---

## 1. コアコンセプト
* **データ構造の極小化:** データベースは「Node（ノード/概念）」と「Edge（エッジ/接続）」の2つのみで構成する。
* **非破壊・柔軟性:** 階層（ツリー構造）を強制せず、多重所属や同名ノードの存在、後からの統合（SameAs）を非破壊に許容する。
* **多用途対応:** 人間関係、お出かけ先、日用品・家電、日付・予定などを同一のグラフ上でシームレスに横断管理できる。

---

## 2. データモデル仕様

### Node（ノード）
```typescript
type NodeType = 'entity' | 'date_day' | 'date_month' | 'time';

type Node = {
  id: string;              // UUIDなどの一意な識別子
  name: string;            // 表示名（例: "組長", "佐藤さん", "2026-08-06"）
  type?: NodeType;         // パーサーによる自動判定（省略時は name から自動判定）
  context_note?: string;  // 識別用の一言メモ（例: "向かいの家", "2丁目"）
  created_at: string;      // ISO8601形式の作成日時
  updated_at: string;      // ISO8601形式の最終更新日時（最近の更新一覧用）
};

```

### Edge（エッジ）

```typescript
type Edge = {
  id: string;
  from_id: string;         // 接続元 Node ID
  to_id: string;           // 接続先 Node ID
  is_same_as?: boolean;    // true の場合、同一概念（SameAs）接続として扱う
  note?: string;           // 接続に添える補足メモ（例: "14:00〜", "おすすめ"）
  created_at: string;
};

```

---

## 3. 主要ロジック & アルゴリズム仕様

### ① 日時ノードの動的フラグ判定（パーサー）

`node.name` の文字列パターン（正規表現）により、タイプを自動判別してUI表示を分岐させる。

* `^\d{4}-\d{2}-\d{2}$` （例: `2026-08-06`） ➔ `date_day`（カレンダーの日にマッピング）
* `^\d{4}-\d{2}$` （例: `2026-08`） ➔ `date_month`（月別まとめハブ）
* `^\d{1,2}:\d{2}$` （例: `14:00`） ➔ `time`（タイムラインソート用）

### ② 同名ノードの作成と検索サジェスト（オートコンプリート）

ユーザーが入力欄にテキストを入力した際、既存ノードを検索してサジェストを表示する。

* **表示ルール:**
* 同名のノードをすべてリストアップする。
* 各候補の横・下に「1歩先の接続ノード名（代表的な1〜2個の文脈）」を薄く注記表示する。（例: `組長 (← 子ども会)`）


* **決定動作:**
* 候補をキーボード/タップで選択 ➔ **既存の Node ID** とエッジを張る。
* 候補を選ばず `Enter` 決定 ➔ 同名であっても**新しい Node** を発行してエッジを張る。



### ③ 非破壊の同等関係「SameAs」接続と表示のマージ

「田中のじいちゃん」と「田中太郎」を統合したい場合、ノードを削除・破壊せず `is_same_as: true` のエッジを引く。

* **Undo処理:** `is_same_as` エッジを1本削除するだけで完全に分離状態に戻る。
* **展開範囲（ホップ数）:** SameAs は**1ホップのみ**辿る。中心ノードと `is_same_as` で直接繋がるノードの非 same-as エッジ群までをマージ対象とし、same-as 連鎖（A≡B≡C）は再帰的に辿らない。反対側ノードの特定は**エッジの所有者ノード基準**で行う（エッジ方向に依存せず正しく表示する）。
* **画面表示（中心ノード展開時）:**
* 対象ノードに直接繋がっているエッジに加え、`is_same_as` で繋がっている先のノードのエッジ群も取得し、統合リストとして表示する。
* `is_same_as` 経由の情報には「(田中太郎 経由)」といったバッジを薄く付与する。



---

## 4. UI/UX 画面仕様

### ① ホーム画面（最近の更新）

* 検索バー未入力時・中心ノード未選択時の結果エリアに表示する。
* ノードを `updated_at` 降順（最近の更新順）で一覧表示する（最大30件）。
* 各行: アイコン、名前、`context_note`、相対時間（例: `3分前`）。クリックで中心ノードとして遷移。
* ヘッダー左上のロゴ（🏷️）クリックでホーム画面に戻る（検索クリア + 中心ノード解除）。

### ② 検索結果

* 検索バーに入力がある間、結果エリアに一致ノードを全件一覧表示する。
* 行クリックで中心ノードに遷移し、検索クエリはクリアされる。

### ③ メインビュー（中心ノード表示・グラフ探訪）

* **ヘッダー:** 全体検索バー（名前で検索、Enterで新規ノード作成）。
* **中心ノード（Current Node）:** 現在フォーカスしているノードを表示。
* 名前（変更可能）、`type` 別のアイコン（`🏷️` entity, `📅` date_day, `🗓️` date_month, `🕐` time）、`context_note`（編集可能）。
* **SameAs 統合:** 現在ノードと別ノードを `is_same_as` エッジで統合する。統合済みノードは「同一視」チップとして表示し、✕で統合解除。
* **ノード削除:** 削除ボタンで確認ダイアログを経てノードと全接続を削除。

* **接続先ノード一覧（エッジで繋がっているノード）:**
* **ハブ表示:** 接続数（エッジ数）が多いノードを優先的・目立たせて上部に表示（親グループ感の自動抽出）。
* **タイムライン表示:** 接続先に `time` タイプ（`14:00`など）が含まれる場合、時刻順にソートして並べる。
* **クイック追加フォーム:** 入力欄から既存ノード検索または新規ノードを作成し、現在の中心ノードと即座に接続。
* **接続解除:** 各行の ✕ で接続（エッジ）を削除。

* **日付ノード:** `date_day` ノード表示時、上部に「＜ 前日」「翌日 ＞」ナビゲーションを表示。対象日付のノードが無ければ新規作成して遷移する。

### ④ カレンダービュー（後続実装・スコープ外）

> 現時点の実装対象外。メインビュー完成後に追加予定。

* 月間カレンダーグリッドを表示。
* `date_day` パターン（`YYYY-MM-DD`）を持つノードが存在する日付マス目に、繋がっているノードの簡易バッジ（タイトルやアイコン）を描画する。
* 日付マス目をタップ ➔ その日付ノード（例: `2026-08-06`）を中心ノードとしたメインビューに遷移する。

> 「前日 / 翌日」ナビゲーションはメインビュー側（③）に実装済み。

---

## 5. 実装への指示

1. **データアクセス層（State / API）:**
* バックエンドはSupabaseを利用する（`@supabase/supabase-js`）。
* 認証はSupabase Auth（メール/パスワード + Google OAuth）を使用し、データは `user_id` カラムと RLS ポリシーでユーザーごとに分離する。
* **許可ユーザー限定:** `allowed_users` テーブルに登録されたメールアドレスのみ利用可能。`is_allowed()` セキュリティ関数で判定し、RLS ポリシーにも適用する。未許可ユーザーには「利用が許可されていません」画面を表示する。
* 全グラフをメモリに保持し（ノード数は少規模前提）、検索・接続一覧・ハブ/タイムライン表示・SameAs マージはクライアント側で導出する。
* スキーマ・RLS・権限は `supabase/migrations/` 配下の SQL ファイルとして提供する（現状 0001〜0004）。

2. **コンポーネント構成:**
* svelte + vite（TypeScript, SPA）を使用する。
* 入力サジェスト（オートコンプリート）付きのノード追加コンポーネント（既存選択 / Enter で新規作成）
* 中心ノードと接続先ノードを描画するメインリストコンポーネント
* ホーム画面（最近の更新）と検索結果一覧コンポーネント
* 月間カレンダーコンポーネント（後続実装・スコープ外）
* 認証画面（ログイン / 新規登録 / Google ログイン）

3. **デザイン:**
* シンプルで清潔感があり、モバイル（スマホ）とPCの両方で操作しやすいレスポンシブなUI（Tailwind CSS）で作成してください。

4. **デプロイ:**
* GitHub Pages にホスティング予定のため、SPA の `vite.config` は `base: './'`（相対パス）でビルドする。
* Google OAuth のリダイレクト先は `VITE_SITE_URL`（未設定なら現在のURL）を使う。Supabase 側の Site URL / Redirect URLs への登録が必要。

5. **実装スコープ（優先順位）:**
* **実装対象:** ホーム画面（最近の更新）、全体検索・検索結果、中心ノード表示（名前変更・context_note 編集）、接続一覧（ハブ/タイムライン）、クイック追加、オートコンプリート（既存選択 / 新規作成）、SameAs 統合とマージ表示、日付ノードの前日 / 翌日ナビゲーション、ノード削除、認証（メール/パスワード + Google + 許可ユーザー限定）。
* **スコープ外:** カレンダービュー、エッジ `note` の編集 UI。

---

上記仕様に基づき、動作する実装コードの作成を開始してください。
